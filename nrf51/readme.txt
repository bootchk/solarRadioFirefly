These files are present in every build configuration, but #IFDEF's conditionally compile them.

Now includes one file from Nordic hal,
renamed from nrf_adc.c => .cpp

Note ADC_PRESENT must be defined.